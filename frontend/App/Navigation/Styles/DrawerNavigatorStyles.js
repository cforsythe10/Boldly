//This is where the styles for the actual drawer go

import { StyleSheet } from 'react-native'
import { Fonts, Colors } from '../../Themes/'

export default StyleSheet.create({
  container: {
  	height: '100%'
  },
  text: {
  	color: '#ffffff'
  }
})